# Frontend Dashboard2 – API lm-backend-vercel (já no Vercel)
# Copie para .env e preencha conforme o ambiente.

# URL do backend no Vercel (produção)
VITE_API_BASE_URL=https://api-mercado-livre-two.vercel.app

# Em desenvolvimento local: use a mesma URL acima para apontar para o back no Vercel.
# Deixe vazio só se o front e a API estiverem no mesmo domínio (ex.: proxy /api).
