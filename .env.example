# URL base da API lm-backend-vercel (Mercado Livre).
# Deixe vazio para usar a mesma origem (ex.: se o front e a API estiverem no mesmo domínio).
# Em desenvolvimento local, use a URL do backend (ex.: https://seu-backend.vercel.app).
# VITE_API_BASE_URL=
